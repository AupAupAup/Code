#include <stdlib.h>
#include <stdio.h>

// -------------------------------------------------------------
// -------------------------------------------------------------

/*Task







*/

// -------------------------------------------------------------
// -------------------------------------------------------------
// F

// -------------------------------------------------------------
// -------------------------------------------------------------
// M
typedef char rubik_t[6][2][2];
int main(void)
{
    const rubik_t Anfang = {
        {{'B', 'B'}, {'B', 'B'}},
        {{'O', 'O'}, {'O', 'O'}},
        {{'G', 'G'}, {'G', 'G'}},
        {{'R', 'R'}, {'R', 'R'}},
        {{'W', 'W'}, {'W', 'W'}},
        {{'Y', 'Y'}, {'Y', 'Y'}}};

    printf("%d\n", (sizeof(Anfang)));

    int a = 1;
    int b = 1;
    int c = 1;
    int d = 1;
    int x = 2;
    int y = 2;
    int z = 2;
    if ((a == b == c == d) && (x == y == z))
    {
        printf("yes\n");
    }
}